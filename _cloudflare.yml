# Cloudflare Pages Build Configuration for Jekyll
# This file is automatically detected by Cloudflare Pages

jekyll:
  version: 4.4

build:
  command: bundle exec jekyll build
  output_directory: _site

